# Dashboard Engine — Project Context

Read this before doing anything. It is the source of truth for architecture and constraints.

## What we're building

**Dashboard Engine**: a desktop platform where designers author and share dashboard packs — skin + layout + personality + voice for a live dashboard that renders straight onto the desktop, Wallpaper Engine style. Users browse, subscribe, install, and customize; the wallpaper thinks, speaks, and (eventually) acts on the machine.

We ship the engine. Designers author the content. We never ship or bundle a copyrighted character.

### Naming

The product was born "AEGIS Voice" and rebranded once its scope outgrew voice. Rules:
- User-visible surfaces say **Dashboard Engine** (titles, tray, docs).
- Internal names are FROZEN legacy and must not churn: the `window.aegis` bridge, `aegis:*` IPC channels. The `aegis-holo` pack id stays reserved (legacy) even though the pack was removed.
- `jarvis` is the DEFAULT built-in pack — the owner's port of their own standalone "JARVIS Dashboard" app (owner's explicit call 2026-07-13; the "no copyrighted characters" rule below is owner-waived for this one pack). The old `aegis-holo`/`ember-archive` references were removed 2026-07-13 (recoverable from git history).
- A **seed gallery** of 8 built-in packs demonstrates range for new users (each clears the JARVIS quality floor; distinct aesthetic + persona + component coverage): `jarvis` (sci-fi HUD, default), `hearth` (warm/cozy serif + embers), `slate` (minimal monochrome), `sakura` (anime spring, petals + soft-sky wallpaper), `pastel-dream` (kawaii, sparkles + pastel wallpaper), `gothic-noir` (crimson/black serif, heavy vignette), `vaporwave` (retro sun-grid wallpaper, scanlines), `neon-cyberpunk` (neon-skyline wallpaper, rain). Wallpapers are procedurally generated (original art, no IP) — see the generator approach in scratch/`gen-wallpapers.js` (minimal zlib PNG encoder). Anime packs are aesthetic-only: NO copyrighted characters in public defaults. Font limit: only 4 built-in stacks (no custom fonts in packs) — palette/shape/ambience carry mood; adding system-font stacks is the lever for more cute/expressive range (still an open option).
- **Ambience effects** (`skin.ambience.effect`, engine-drawn particle layer, reduced-motion safe): none, embers, dust, snow, petals (sakura), rain (cyberpunk), sparkle (twinkling stars, pastel). Colour comes from a palette key per effect; density 0.05–1.
- **QUALITY FLOOR (owner directive 2026-07-13): the JARVIS pack is the bare minimum for design and component usability. Nothing ships — no component, no reference pack, no default — that looks or works worse than it.**
- `.dpack` is the pack extension; legacy `.aegispack` installs forever. `DE_*` env vars are canonical; legacy `AEGIS_*` still honoured. User data migrates from the old `aegis-voice` dir automatically (lib/paths.js).

## Current state (as of 2026-08-04; M1–M3 + shell + planner + launcher + JARVIS default + Module SDK + Workshop publish+consume + performance citizenship + security hardening + BYO assistant + PACKAGING + creator hub + from-scratch builder + photo gallery + global weather + prod hardening + interactive calendar + time-picker + persistent assistant chat panel + sentence-clip voice + background music + ring-clock + Workshop browse filters + now-playing + audio visualizer + video wallpapers + layered parallax backgrounds + WebGL background effects + assistant persona presets + ALL-MeloTTS HD voice bank en/es/fr/zh/ja/ko (Piper removed 2026-08-03) + onedir HD engine (~13 s cold vs ~42 s) + voice pre-warm + per-language voice presets + wallpaper-attach retry/DPI-safe launch + Chinese speaker-id fix + reasoning-model token-budget fix + emoji/markdown/think strip before TTS + persona↔voice language auto-match + MALE voices via Piper for en/es/fr (VCTK p226 / davefx CC0 / UPMC-pierre) with Female↔Male swap; zh/ja/ko female-only + assistant reply streaming)

**✅ MALE VOICES — decided + shipped 2026-08-04 (owner chose "ship en/es/fr males now").** Every Latin-script language now has a real, floor-clearing male: `en_male` = Piper VCTK p226 (CC-BY-4.0), **`es_male` = Piper davefx (CC0)**, **`fr_male` = Piper UPMC speaker-1 pierre (CC-BY-SA-4.0)** — new `es-male`/`fr-male` presets, Female↔Male chosen by picking that language's preset. Verified: both synth through the real piper binary (exit 0) with objective F0 checks (es 122 Hz, fr-pierre 126 Hz = male; fr-jessica speaker-0 = 230 Hz female, so speaker:1 is correctly the male). **zh/ja/ko stay FEMALE-ONLY** — the decision, not a TODO: no commercially-licensable male neural TTS covers Mandarin/Japanese/Korean. Researched 2026-08-04: **Kokoro** (Apache-2.0) *does* have zh/ja males but at its own Grade C-/D — below the JARVIS floor — and has **zero Korean**; every Korean-capable open model is either voice-cloning (violates our no-sample rule; XTTS also non-commercial) or a GPU-only 3B LLM (Orpheus). So adding a whole second engine buys sub-floor CJK males and still no Korean → not worth it. Manager voice hint now states male = en/es/fr, zh/ja/ko female-only. Revisit if a licensed, floor-clearing male CJK/Korean model appears. See [[all-melotts-voicebank-plan]] for the per-language state + the Kokoro/Orpheus audit.

**✅ HD HOSTING MIGRATED (2026-08-05):** the 6 MeloTTS packs + the onedir engine zip are now hosted on the NEUTRAL org repo **`dashboard-engine/dashboard-engine-hd`** (off the personal jh03 account); `voices.json` URLs swapped (sha256 pins unchanged — same files) and verified (each pack's smallest file hashes to its pin from the new host, engine+checkpoints HTTP 200). This also resolved the old "re-run upload_hd_voice.py" pending item — the current onedir engine zip is what got uploaded. The `en_male`/es/fr Piper males still come from public rhasspy/piper-voices (no owner upload). Old jh03 repo can stay as a fallback until go-live, then be deleted.

**🟢 STEAM APPID LIVE (2026-08-05): Dashboard Engine = AppID `5066330`** (Steam Direct app created; product type Software Application, paid). Baked into `lib/workshop.js resolveAppId` as the default (was Spacewar 480); `steam_appid.txt` set to 5066330 (dev-only, excluded from the shipped package). Owner must still **enable Community Workshop** for the app in Steamworks. First test publish hit **`k_EResultBusy`** ("method busy") — EXPECTED on a brand-new app while Steam provisions its Workshop backend (minutes→~a day); retry later. `lib/workshop.js` now surfaces that as a friendly message (edited; commit pending shell classifier). 30-day gate applies to the public STORE release only (from payment date), NOT to Workshop/dev testing.

**⭐ NEXT BUILD (approved 2026-08-05, starting after owner compacts): "PUBLISHED DASHBOARDS" creator-management feature.** A "Published" tab (Manager) to SEE/EDIT/UPDATE your Workshop dashboards, MACHINE-PORTABLE via Steam identity (`getUserItems` + "Get editable copy" that downloads+imports your own item and restores the update mapping, gated by creator SteamID), with a **STRICT publish gate**: only from-scratch **builder** packs + your own re-downloaded items are publishable; imports-from-others, forks of built-in seeds, and the seeds themselves are BLOCKED (owner's call, for long-run quality). Two security layers (app only wires the update-mapping for items Steam certifies you own; Steam server-side rejects updateItem on non-owned items). **Full file-by-file plan + provenance model (`.aegis-meta.json origin`) + manual-testing checklist is in [[published-dashboards-plan]] — READ IT before building.** Integration points found: tabs in manager.html (`tab-installed/browse/create/...`), pack provenance = `p.origin` + `packstore.readMeta`, editor via `hooks.openEditor(id)`, `aegis:builder:create` (ipc.js ~789), import via `workshop.importSubscribed`.

**✅ VOICE SHIPPING — DECIDED 2026-08-05 + enabling code shipped; owner does the build.** The owner chose: **(1) bundle the MeloTTS engine + BOTH English voices** (female `en_hd` + male `en_male`) in the Steam depot so first-run speaks with a neural voice instantly and the default JARVIS pack is fully voiced (~+2.7 GB depot); es/fr/zh/ja/ko stay ON-DEMAND. **(2) host the on-demand voices on a NEUTRAL HuggingFace org** (off the personal `jh03` repo; sha256-pinned so the host is interchangeable). Enabling code DONE + tested: `lib/voicebank.resolvePiperModelPath` (the Piper read-side depot fallback — the one gap; engine/`findEngine`, MeloTTS/`resolveModelDir`, `findPiper` already checked `appRoot`), `ipc.synthClip` uses it, and `package.json` `build.files` un-excludes `voices/en_hd/**` + `voices/en_GB-vctk-medium.onnx(.json)` + `bin/melotts_full/**` + `bin/piper/**`. Verified: a depot-`appRoot` resolves engine+en_hd+en_male with "downloaded wins" precedence (8/8 test). **OWNER TO DO** (see BUILD.md): stage the large files into the repo (gitignored), `npm run pack`, upload the depot; create the HF org + upload + I do the `voices.json` URL find/replace. **LICENCE NOTE:** bundling `bin/piper/` ships espeak-ng-data (GPLv3) → a source-offer obligation; dropping the two piper/vctk `build.files` lines makes the English male a ~100 MB on-demand download instead (female HD still bundles clean, MIT). Also still ⚠️ pending: re-run `upload_hd_voice.py` for the engine zip. See [[all-melotts-voicebank-plan]].

**Recent (2026-08-01), all shipped + pushed:** (1) **Assistant persona UX** — a preset picker for the system prompt (JARVIS/Nova/Sage/Ace/Pip + localized es/fr/de/pt) so non-AI-savvy users pick a character; users can **write + save their own named personas** (`personaPresets` in assistant.json, chips to use/delete); **Test connection now speaks the reply** (model introduces itself, PCM played in the manager) to prove model+voice together; clarified the spoken-reply voice flow (the assistant uses the profile picked in Manager→Assistant→Voice, NOT "the last thing you tuned"). (2) **Multilingual voice bank** — 8→16 voices, adding German/Spanish/French/Portuguese/Vietnamese/Turkish/Ukrainian/Polish (all CC0 or CC-BY-4.0, licence-audited + sha256-pinned, verified end-to-end). See the Voice bullet for the hard reality on Japanese/Korean/Hindi/etc. (3) Phase-C refinements: crisp adjustable cursor-ripple; library cards are now cached static snapshots (no live rendering). **Japanese + Korean voices SHIPPED (2026-08-03)** as part of a full pivot to an **ALL-MeloTTS voice bank** (en/es/fr/zh/ja/ko; all 16 Piper voices removed). Then (2026-08-04): onedir HD engine, voice pre-warm, per-language voice presets, a raft of assistant fixes (Chinese speaker-id, reasoning-model token budget, emoji/markdown strip, persona↔voice auto-match), and a **male voice** (Piper VCTK p226, English Female/Male swap — Piper re-enabled). See the Voice bullet + the ⭐NEXT / ⚠️PENDING notes above and [[all-melotts-voicebank-plan]].

Dashboard Engine is a working Wallpaper-Engine-class product: designers publish packs, users subscribe/install/edit them, the active pack renders straight onto the desktop. Git log tells the full story milestone by milestone. Repo pushes to https://github.com/jhong03/Dashboard-Engine (origin/master) — push every commit.

**Recent (2026-07-26 → 07-28), all shipped + pushed:** interactive desktop calendar (click-a-day reminder popover w/ hour/min/AM-PM spinners); assistant chat is now a persistent, draggable, scrollable PANEL with transcript history (`assistant-thread.json`); long TTS replies synthesized sentence-clip by clip (`splitForSpeech`) to kill loudnorm drift; **background music** (user's own local files, Settings-configured, streamed by opaque id over `demusic://`, pauses on full-screen/battery); **ring-clock** (themeable centrepiece); Workshop **Browse filter sidebar** (faceted tags + auto Contains-Code/Needs-AI); **now-playing** widget (WinRT SMTC, any player, excludes our own audio); **audio visualizer** (system-audio loopback → bars/waveform/radial/bloom; bloom full-bleed = ambience). Media roadmap: lyrics DEFERRED (licensing), Spotify Web API OAuth optional-later. **NEXT (owner): AppID is PAID FOR — blocked on Steam tax/identity verification (2–7 business days); owner will ping when done.** Final check-up ran green (smoke/selftest/8-pack validate/CSP) and `/dist` was rebuilt fresh so it includes now-playing + visualizer (verified: `steam_appid.txt` absent from payload, steamworks win64 binding + new media scripts/lib all packaged; still no AppID baked → Spacewar 480 until rebuilt with the real one). See [[steam-appid-rebuild-plan]] for the exact once-verified steps (enable Workshop, set DE_STEAM_APPID + steam_appid.txt, `rm -rf dist && npm run dist`, test publish, strip the `[workshop]` diagnostics once green). Decisions this session: code signing SKIPPED (Steam ships the depot, not the NSIS installer, so SmartScreen never applies); browse-card previews STAY static thumbnails (the "real-render previews" idea is dropped — too costly). Optional un-started follow-up: drag-drop `.dpack` import onto the Manager window (preload `webUtils.getPathForFile` → install-from-path IPC → existing zip-safe installer).

**LIVING BACKGROUNDS program (owner-approved 2026-07-31; ship before the public Steam store release; WebGL approved, no new deps, no build step). Phase A = VIDEO WALLPAPERS — SHIPPED + pushed 2026-07-31 (awaiting owner test sign-off before Phase B).** `skin.wallpaper` may now be a video (`assets/*.mp4|webm`, ≤30 MB) as well as an image; `skin.wallpaperVideo.playbackRate` (0.25–2); `wallpaperFit`/`PosX`/`PosY` apply to video too. ALWAYS muted (pack audio is out of scope — background music stays the separate user-owned feature). Video NEVER enters the base64 `assets` map: it streams over a new gated **`depack://`** scheme (clone of `demusic://` — `lib/videostore.js` maps an opaque sha256-of-path id → validated path; `main.js` `protocol.handle` forwards only Range; wired into `aegis:packs:load` so desktop/editor-stage/manager-preview/shot all get the url through one point). Renderer (`components.js applySkin`) mounts a `<video class="wallpaper-video">` in the wallpaper slot (z-index:0, behind textures/ambience/components); `setWallpaperPlayback()` is called from `applySkin`(play)/`freezeAmbience`(pause) so dashboard.js's freeze/resume drives it with NO new wiring; reduced-motion OR a static-thumbnail surface shows frame 0 paused; a decode error falls back to palette void (never a black desktop). CSP: `media-src depack:` added to dashboard/editor/manager/shot (module iframes stay `default-src 'none'` → can't reach it). Video travels in the `.dpack`: `lib/zip.js` admits `mp4|webm` with a video-aware 30 MB per-entry cap (images keep 5 MB) + 55 MB total, inflate ceiling per-entry so the video cap never widens the image bomb ceiling; `lib/packstore.js` uses one `ASSET_FILE_PATTERN` (image+video) for export/create/save; Workshop `cpSync` carries it unchanged. Editor Skin tab + builder Background step gained **Import/Choose video…** (staged main-side, returned as a depack:// url via `videostore.registerStagedVideo` so the stage plays it live) + a video-only playback-speed slider. Shot/preview waits for the video's first decoded frame (`loadeddata`, 5 s cap) so Workshop previews aren't black. `packs -- validate` reports video sizes / flags oversize; `smoke` has a fail-soft `[4/4]` video-pipeline assertion. VERIFIED with real screenshots (isolated `--user-data-dir` instance run alongside the installed app — see machine note below): both the live desktop and the offscreen preview render the video wallpaper with components composited on top. **Machine gotcha: the owner's INSTALLED `Dashboard Engine.exe` (from `/dist`) holds the single-instance lock, so a dev `electron .` bounces off it and quits in ~3 s; run tests with `--user-data-dir=<temp>` to get an independent instance instead of killing their running wallpaper.**

**LIVING BACKGROUNDS Phase B = LAYERED PARALLAX BACKGROUNDS — SHIPPED + pushed 2026-07-31 (awaiting owner test sign-off before Phase C).** `skin.background = { layers[1-6] (image/video, back-to-front, each with depth 0-1 / fit / posX/Y / opacity / drift{x,y}), parallax{strength 0-2, axis both|x|y} }`. The wallpaper is now the ONE-layer case: the sanitizer normalizes `skin.wallpaper` ↔ a single depth-0 layer (both directions, one place), so the renderer consumes ONLY `background.layers` and every legacy pack renders pixel-identically (a motionless depth-0 layer gets no scale/transform — verified: neon-cyberpunk + 8 seed packs unchanged). Renderer (`components.js`): `applyBackground` builds a `.bg-stack` (z-index:0) of `.bg-layer` elements (image divs / muted looping videos), reused across re-renders when the source set is unchanged (no video restart on resume/prop-tweak); parallax follows the pointer (one lerped listener per surface, axis-limited), drift is a slow bounded sine, and each moving layer is scaled up just enough that translation NEVER exposes an edge (a motionless layer stays scale 1). CRITICAL raf refactor: `applyAmbience` was split — `setupAmbienceParticles` builds the canvas/particles, and `applyAmbience` now owns the ONE shared raf that ticks BOTH particles and background motion (never a second raf), honouring the fps cap + freeze; `freezeAmbience` pauses the loop + all video layers. `applyBackground` also FALLS BACK to `skin.wallpaper` when a pack has no `background` (the builder's unsanitized live-preview pack). Global user control: `settings.getBackgroundMotion/setBackgroundMotion` (`{parallax:0-1}`, default 1) in settings.json → IPC `aegis:settings:backgroundMotion` → broadcast `aegis:desktop:backgroundMotion` → dashboard passes `opts.parallaxMultiplier` (effective strength = pack × user); Manager → Settings has a "Background motion" slider (incl. Off). Editor Skin tab: a full **Background layers** editor (per-layer card: reorder/remove, fit/pos/depth/opacity/drift, add image/video ≤6, global video-speed, parallax strength/axis; the stage animates + follows the pointer while authoring). Builder Background step: an optional **Depth layers** expander (extra layers in front of the base wallpaper, depth slider + parallax strength; `syncBuilderBackground` composes `skin.background`). `builder:create` now carries EVERY referenced staged asset (wallpaper + layers + component images), not just the wallpaper. Video/`.dpack` carrier + Workshop unchanged (layers are just more `assets/` refs; `collectAssets`/`collectVideoRefs` walk layers). PACKS.md documents it.

**LIVING BACKGROUNDS Phase C = WEBGL BACKGROUND EFFECTS — SHIPPED + pushed 2026-07-31 (the whole 3-phase program is now done — awaiting owner test sign-off).** `background.layers[].effects[≤3]`: the 5 shader effects **ripple / sway / drift-warp / pulse / cursor-ripple**, each with clamped params + an optional **region** (rect/ellipse + feather) and image **mask** (multiply). New renderer-side module **`src/gl-background.js`** (`window.AegisGL`, loaded before components.js in all 4 pages): when any layer has effects AND WebGL is available, the WHOLE stack renders through ONE GL canvas (layers as textured quads, back-to-front, premultiplied alpha) — parallax/drift are the SAME per-layer math as uniforms (identical motion), one fragment shader per layer generated with `#define`s (GLSL ES 1.00 → runs on WebGL2+WebGL1), cursor-ripple via a ≤12 ring uniform buffer, regions/masks sampled in-shader, video layers textured from a hidden source `<video>` each tick, oversize textures downscaled to MAX_TEXTURE_SIZE. Effect-displacing layers overscan a touch so cover edges never smear void; `contain` letterboxes. **Fail-soft ladder:** no context / all shaders fail / 2 `webglcontextlost` events → the surface disables GL and `applyBackground` rebuilds the DOM stack. `DE_NO_GL=1` (→ `?nogl=1` → `window.__DE_DISABLE_GL`) forces the DOM path — a real escape hatch AND the reliable way to verify the fallback (Chromium's `--disable-webgl` is IGNORED under ANGLE on this machine, so don't rely on it). The shared ambience raf drives the GL handle (`setParallax`/`setPointer`/`tick`); `freezeAmbience` stops ticking (last frame persists), `destroy` releases textures+context (1-per-surface budget), reduced-motion/static draws one frame + no loop. Gallery mini-render cards pass `opts.noGL` so N cards never allocate N GL contexts (they show a static DOM thumbnail). The GL rebuild `sig` tracks only structural shape (effect types + region shape + mask + pulse-tint + still + strength) so editor param-slider drags update LIVE via shared-object uniforms (no shader recompile); a type/region/mask change recompiles. Editor Skin tab: per-layer **Effects (WebGL)** section (add/reorder/remove, param sliders, pulse tint, optional numeric region; a "preview unavailable" note if the GPU lacks WebGL). Region drag-on-stage authoring DEFERRED (numeric v1). Seeds refreshed (`vaporwave` drift-warp+pulse, `sakura` mist+ripple, `neon-cyberpunk` pulse+cursor-ripple over its rain ambience; other 5 untouched). PACKS.md has a Background-effects chapter; Creator hub updated. VERIFIED with screenshots (effects render full-bleed, tasteful; DOM fallback clean, no crash; seeds tasteful). **Refinements 2026-08-01 (owner feedback):** cursor-ripple is now a crisp expanding gaussian ring (not a slow standing distortion) with an adjustable `speed` param (0.2–3, default 1.4); and library gallery cards became cached static snapshots (see the Manager entry) — the `opts.noGL` gallery-card path is moot now (cards don't render at all). **NEXT: owner test sign-off. Then the whole Living Backgrounds program (A+B+C) needs `rm -rf dist && npm run dist` before the Steam depot upload.**

**ROAD TO A PAID STEAM LAUNCH (flat ~$3.99; owner's choice — Steam ownership = the paywall, no gating code). The entire launch-blocker list is now DONE — what remains is EXTERNAL/business, not code.** Working cadence with the owner: **build ONE phase → STOP → give a test checklist → owner verifies → next.** Shipped across the road-to-Steam program: Phase 1 security hardening (ffd2647); Phase 2 assistant→BYO OpenAI-compatible (1d1f29a, local qwen confirmed); out-of-box correctness bugs (countdown/weather/battery/AudioContext); launcher scroll; Settings screen + start-with-Windows + quiet-launch-at-login; battery "AC power" on desktops; multi-monitor "choose display" (DPI-safe positioning in desktop-attach.ps1); per-pack user properties (`props`/Customize knobs); first-run welcome/onboarding; **crash reporting** (lib/logger.js + crashReporter local, no upload); **PACKAGING** (electron-builder → NSIS installer + `dist/win-unpacked` Steam-depot payload; `asar:false`, `signAndEditExecutable:false` to dodge winCodeSign symlink extraction on non-admin Windows; see BUILD.md); **in-app Creator hub** (Create tab: guide + component/skin reference + actions + PACKS.md); **from-scratch guided BUILDER** (Manager → Create → "Start from scratch": Background/Colours/Particles/Typography/Components(+layout auto-place)/Persona/Customize-knobs/Name → creates pack → opens editor, or "Create & publish"; wallpaper crop/fit); **Photo Gallery component** (`gallery` — looping slideshow of pack image assets); **Workshop consume side** (Browse tab: browse/subscribe/import in-app, previews fetched main-side as data URIs); **global weather location** (Settings → Weather geocodes a city; any unset-location weather component falls back to it — seed packs no longer hardcode a city); **production hardening** (voice/piper downloads → %APPDATA% so updates don't wipe them + one-time migration; fixed a MaxListeners leak in resetPackWatcher); **editor prop-authoring** (Customize tab); **seed-pack polish** (all 8 expose knobs, hostname off); a **9-issue QA sweep** fixed (notably: Workshop import now records workshopId so "In library" works).

**Steam publish is verified working** end-to-end (pipeline: stage stable content+preview under %APPDATA%/workshop-staging → validate/adopt/create item → updateItem). The ONLY blockers hit are the shared **Spacewar test app (480)** limits: an item-COUNT cap (`createItem`, not time-based — delete items on the web) and an upload RATE limit (`updateItem`, time-based — wait ~15–30 min). Both vanish on a real AppID. AppID is now **configurable via `DE_STEAM_APPID` env** (+ matching `steam_appid.txt`); default 480. NOTE: temporary `[workshop] …` diagnostic console.logs are still in lib/workshop.js — STRIP them once the owner has a real AppID and sees a clean green publish.

**Remaining before go-live = EXTERNAL only:** Steam Direct ($100) is PAID + all onboarding paperwork submitted (2026-07-28; account `infinite_abyss17`); waiting on Valve's tax/identity verification (2–7 business days) → then the AppID appears, **enable Workshop for it** (not automatic) → set `DE_STEAM_APPID` + `steam_appid.txt` → rebuild + test publish. Separate ~30-day Steam Direct trust period gates the public STORE release (not the Workshop test). Code signing decided AGAINST (Steam ships the `win-unpacked` depot, not the NSIS installer). Open follow-ups the owner may want next: **drag-drop `.dpack` import** onto the Manager window (approach agreed, not built — see [[steam-appid-rebuild-plan]]); the Living Backgrounds program (Phases A+B+C ALL SHIPPED 2026-07-31 — video wallpapers, layered parallax backgrounds, WebGL background effects — see the Current-state entries; awaiting owner test sign-off). (Assistant reply streaming SHIPPED 2026-08-05.)

**Confirmed 2026-07-13:** clicks reach the wallpaper-layer window — launcher tiles launch apps/files and focus windows on the user's machine. Interactive wallpaper components are a proven pattern.

**Windows & surfaces**
- **Desktop surface** (`src/dashboard.*`) — chromeless window (`skipTaskbar:true`) reparented under the shell's wallpaper layer (`scripts/desktop-attach.ps1`, handles classic WorkerW and 24H2 Progman; `--no-desktop` or non-Windows falls back to a normal window). Narrowest preload in the app. **Launch-attach robustness (fixed 2026-08-03):** the shell's wallpaper WorkerW may not exist the instant the app launches (esp. quiet-launch-at-login), so a single attach could miss and drop to a TASKBAR window — `createDashboardWindow` now RETRIES the attach up to 5× (600 ms apart) before that fallback. And it ALWAYS passes the chosen monitor's index (even when unpinned = primary), so the window is positioned with DPI-safe PHYSICAL-pixel rects (the old `-1` unpinned path left Electron's DIP bounds → not-quite-fullscreen on high-DPI). A null `displayId` stays the valid "Auto/follow-primary" choice — we do NOT auto-pin (it would fight the Settings "Auto" option).
- **Manager** (`src/manager.*`) — THE app window: library gallery (cards are cached STATIC snapshot images (main renders each pack offscreen with DEMO data → `%APPDATA%/…/thumbnails/<id>.png`, regenerated on save by pack.json mtime; `main.getPackThumbnail` + `aegis:pack:thumbnail`; no more live-rendering cards — the old realThumbInto/cardPreviews path was removed 2026-08-01); the detail sidebar runs a LIVE in-motion preview at the display's aspect; blueprint/monogram only as fallbacks for unloadable/remote packs), Browse tab (registry feeds: subscribe / install / update, sha256+size-pinned downloads), Planner tab (Google-Calendar-style month grid: click-a-day quick-add, event chips, edit modal, repeats daily/weekly/monthly/yearly, per-event alert lead time), Launcher tab (pin apps from a Start Menu picker, files/folders via main-side dialog, reorder).
- **Editor** (`src/editor.*`) — WYSIWYG pack editor: palette drag&drop, move/resize/z/duplicate, three-tab inspector (component options, skin tokens, persona), image import (dialog + staging in MAIN only), fork-on-save (editing built-in/registry packs copies them; `packstore.saveEdited`). Stage uses the real display aspect ratio.
- **Voice panel** (`src/index.html` + `renderer.js`) — the M1 tuning panel as a tool window (`npm run panel`; isolated Chromium profile so it runs beside the engine).
- **Tray** — the engine lives here: Open Manager / Voice Tuning / Switch Pack (radio) / Pause-Resume Desktop / **Performance** (submenu: pause on full-screen apps, pause on battery, fps cap 24/30/48/60) / Quit. Closing windows never quits engine mode.
- **Performance citizenship** (shipped 2026-07-15): the animated wallpaper pauses/throttles like Wallpaper Engine so it's a good 24/7 citizen. `lib/presence.js` runs ONE long-lived PowerShell watcher (`scripts/fullscreen-watch.ps1`; compiles the P/Invoke once, cheap polls every 3 s, emits FULLSCREEN/NORMAL on change) → main folds that with `powerMonitor` battery state + the user's prefs (`settings.getPerformance`: `pauseOnFullscreen` default on, `pauseOnBattery` default off, `maxFps` default 30) into `aegis:desktop:power {active,maxFps}`, sent on did-finish-load + every change. The desktop renderer (dashboard.js) caches the pack; `active:false` → `renderer.destroy()` + `AegisComponents.freezeAmbience(body)` freezes the last frame at ~0 CPU; resume re-renders from cache; `maxFps` caps the ambience raf loop. FAIL-SOFT: non-Windows / spawn fail / watcher death → never full-screen, wallpaper keeps running. Manual tray Pause still just hides the window. **DETECTION (fixed 2026-07-19):** freezes only when a real window actually COVERS THE PRIMARY MONITOR (foreground-window geometry vs the primary `GetMonitorInfo` rect; excludes the shell classes + our WS_CHILD reparented window). NOT `SHQueryUserNotificationState` — that reported "busy" for any background full-screen app (a game left running), which froze a fully-visible wallpaper on its empty pre-telemetry frame (the "everything shows –, stuck" bug), and it also missed borderless full-screen. Foreground-covers-primary catches borderless too and resumes the instant you're back on the desktop; a full-screen app on a secondary monitor doesn't pause the primary wallpaper.

**Shared foundations**
- `src/components.js` — ONE renderer for desktop + editor (that's what makes the editor exact). Component types: status, clock, analog-clock, hud-clock (reactor rings, sci-fi), ring-clock (themeable centrepiece — `minimal`/`halo` styles, palette + display-font, the non-sci-fi counterpart to hud-clock), nowplaying (SHIPPED 2026-07-27 — the Windows media session: current track from Spotify/browser/ANY player via WinRT SMTC `scripts/media-nowplaying.ps1` + `media-control.ps1` + `lib/media.js`; album art + title/artist + interpolated progress + play·pause·next·prev; EXCLUDES our own background-music `<audio>` session by AppUserModelId `com.dashboardengine.app`; art is best-effort — the Store Spotify app opens an empty thumbnail so it falls back to a ♪ placeholder; personal data, desktop-only, never in a pack), visualizer (SHIPPED 2026-07-27 — audio spectrum reacting to ANY system audio via getDisplayMedia system-audio LOOPBACK → one shared AnalyserNode → styles `bars`/`waveform`/`radial`/`bloom`; a `bloom` placed full-screen behind other components is the reactive "ambience"; desktop-only + fail-soft — reduced-motion/editor/denied-capture show a quiet static idle; capture may need a first user gesture; see the display-capture security note in Module SDK), stats, cores (per-core bars), sysinfo (key/value readouts incl. uptime/host), meter, sparkline, text, image, divider, calendar (INTERACTIVE on the desktop — click a day to add/complete/remove reminders in place via a small glass popover with hour/minute/AM-PM time spinners; editor/manager previews stay static read-only), countdown, weather (Open-Meteo via main, keyless; `compact` one-line strip), agenda, notifications (live Windows toasts via WinRT UserNotificationListener in main, `scripts/notifications-list.ps1`; personal data, fail-soft if access not granted), launcher (user's pinned/recent/open apps as clickable tiles; opaque-id launch allowlist in main, `lib/launcher.js` + `scripts/windows-list.ps1`/`window-focus.ps1`), assistant (console line that opens the AI chat on click), module (SHIPPED 2026-07-15 — designer-authored sandboxed HTML/CSS/JS; see Module SDK below). Telemetry binds: cpu, mem, disk, battery (+ coresPercent/uptimeSec/hostname in the stats sample).
- **Module SDK** (shipped 2026-07-15; the once-out-of-scope "sandboxed HTML packs", now the owner's explicit call): the `module` component runs untrusted designer HTML/JS in an `<iframe sandbox="allow-scripts">` (opaque origin) loaded from a custom `demodule://` scheme (main.js) — a custom scheme does NOT inherit the trusted page's strict CSP the way srcdoc/data: do, so the frame runs its own inline code under a served, NETWORK-LESS CSP (`default-src 'none'`; no connect/fetch/ws; imgs/fonts data: only; no eval). The whole wrapped doc rides in the frame URL as base64url; main echoes it back (never parses/runs it). Code is stored inline in `options.html` (≤24 KB; manifest cap raised to 256 KB). The engine↔module channel is postMessage-only and ONE-WAY push: theme tokens (`--de-*` CSS vars + `DE.onTheme`) + public system telemetry (`DE.onData`, NO hostname/personal data) + pack images (`DE.asset`) — the module can never drive the engine. CRITICAL preload hardening: every pack-hosting preload (dashboard/editor/manager) now exposes the `aegis` bridge ONLY when `window===window.top`, so a module subframe can't reach IPC. `frame-src demodule:` added to the three page CSPs. Authored in the editor (Custom module palette entry → live code box). **Security hardening (2026-07-25, verified via hostile probe):** IPC/Node/cross-origin/storage/cookies/eval/fetch AND now camera/mic/geolocation, WebRTC (STUN/DNS exfil channel), `window.open`, and navigation are all blocked; theme + live stats still work. Mechanisms: `session.setPermissionRequestHandler/setPermissionCheckHandler` deny ALL web permissions engine-wide (main.js whenReady) EXCEPT `display-capture` AND `media` — the visualizer's system-audio loopback needs BOTH (the audio track triggers a `media` check) — which the REQUEST handler grants ONLY to the desktop window (`isDesktopWc`: wc===dashboardWindow.webContents; the check handler is coarse since its wc is unreliable, so the request handler + display-media handler are the authoritative gates). `setDisplayMediaRequestHandler` then restricts the actual stream to the `/dashboard.html` TOP frame (`frame.top===frame`), pairing a `desktopCapturer` screen source with `audio:'loopback'` (Chromium requires a video source even for audio-only; the renderer stops the video track immediately). A pack `module` iframe is thus triple-blocked: `allow=""` Permissions Policy + the SDK getUserMedia shim + the display-media frame gate. `app.on('web-contents-created')` sets `setWindowOpenHandler(deny)` + `will-navigate`/`will-redirect` preventDefault on every frame; the module SDK shim neuters `RTCPeerConnection`/`getUserMedia` before designer code runs; the module iframe carries `allow=""`. Docs: PACKS.md → Module SDK.
- **AI assistant / LLM bridge** (`lib/assistant.js` + the `assistant` component in `src/components.js`): BYO OpenAI-compatible endpoint (pivoted 2026-07-25 — the old keyless Pollinations default started returning 402/payment-required, and a shared free endpoint has no SLA for a paid worldwide launch, so it's now bring-your-own: NO single point of failure, NO owner cost). One provider (`openai`): a Base URL + OPTIONAL key + Model id. Free-to-user paths remain: a LOCAL model (Ollama/LM Studio — no key, offline), a free-tier key (OpenRouter/Groq), or the user's own OpenAI key. `isConfigured` = model set AND (a key OR a base URL); an unconfigured `ask` returns a friendly "set it up in Manager → Assistant" message (not an error); HTTP 401/402/404/429 are rewritten to plain guidance (e.g. 402 → "out of credits, use a free-tier key/local model"). Any key entered is stored OS-encrypted via Electron `safeStorage`, never exposed to a renderer (only `hasKey`) or written into a pack/export. Configured in Manager → Assistant tab (BYO-only UI: Base URL + key + Model id, each with a setup guide; provider dropdown + live free-model list REMOVED). The **persona (system prompt)** field has a **preset picker** (2026-08-01) — JARVIS/Nova/Sage/Ace/Pip, each baking in the read-aloud rules (plain sentences, no markdown/lists/emoji) — plus an inline tip, so non-AI-savvy users pick a character instead of writing a prompt (`ASSISTANT_PRESETS` in src/manager.js). Users can also **write their own prompt and save it as a named persona** ("Save current as a preset…" → chips under the dropdown, click to use / × to delete; persisted in assistant.json `personaPresets`, `assistant.addPersonaPreset/removePersonaPreset`, `aegis:assistant:preset:add/remove` — saveConfig preserves the list). **Test connection** now also SPEAKS the reply (the model introduces itself; `aegis:assistant:speak` → PCM played in the manager) when "speak aloud" is on, so one test proves model + voice together. Power users still edit the filled text. The `aegis:assistant:models` IPC is now a no-op stub. Legacy note: the removed default was Pollinations `text.pollinations.ai`. The chat is an IN-PACK COMPONENT (`assistant`), NOT a separate window. PRESENTATION redesigned 2026-07-26 (the old inline `<input>` + upward-expanding reply log overlapped neighbouring components, got wiped on every pack re-render, and had no history): the wallpaper now shows a compact console prompt line that acts as a TRIGGER; clicking it opens a **persistent chat panel** — a floating, DRAGGABLE (by its header), SCROLLABLE glass overlay on `<body>` with a header (title / Clear / ×), the full transcript, and the input docked at the bottom. The panel is a **renderer-scoped singleton** (built once, deliberately NO `live.disposer`) so it survives the frequent desktop re-renders (launcher/reminder repaints, freeze/resume) that used to erase the conversation. Real HISTORY/MEMORY: the transcript is persisted to disk (`lib/assistant.js` `loadThread`/`saveThread`/`clearThread` → `assistant-thread.json`, personal data, never in a pack) and exposed via `aegis:assistant:history`, so the panel restores the conversation after a re-render AND across app restarts. To let the input take keyboard focus the desktop window is `focusable:true` (shown `showInactive` so it never steals focus at launch). The OLD docked-window system (`createAssistantWindow`/`syncConsole`/`resizeConsole` + `src/assistant.*` and `preload-assistant.js`) was REMOVED once and for all. Editor/manager previews render the component as a static prompt line (no `services.assistant` there). Replies are spoken via `aegis:assistant:speak` (piper/dsp with the saved profile); conversation state lives in main (`aegis:assistant:ask`, `:reset`, `:history`). **Reply STREAMING (2026-08-05):** `assistant.ask(userDir, messages, onDelta)` streams the reply via a `stream:true` SSE request; the ask IPC pushes `aegis:assistant:stream {id, start|delta|done|error}` to the ASKING window (`onAssistantStream` preload subscription → `services.assistant.onStream`), and the chat panel appends tokens LIVE to the pending bot bubble (single-in-flight, so any delta belongs to the current reply; the resolved `{ok,text}` is authoritative and reconciles the final trimmed text). `<think>` chain-of-thought is hidden mid-stream via `visiblePrefix` (even a `<think>` tag split across chunks stays hidden) and `delta.reasoning`/`reasoning_content` are ignored; an endpoint that IGNORES streaming (returns one JSON body) fails soft to the one-shot `parseOpenAIShape`; the fixed 60 s timeout became an IDLE timeout (reset per chunk) so a long local-model generation isn't cut off mid-stream. The stream push is harmless to non-subscribers (the manager Test button still uses the returned `{ok,text}`). Speech still fires ONCE on completion (the existing sentence-clip pipeline, unchanged) — per-sentence-during-stream TTS is a possible follow-up. VOICE FIX (2026-07-26): long replies are synthesized SENTENCE-CLIP by sentence-clip (`splitForSpeech`, ~180 chars/clip) and the PCM joined — a long reply run through the DSP as one clip drifts/distorts because the always-on single-pass `loudnorm` rides its gain across long audio; short clips don't, so keeping every clip short (like a tuning clip) fixes it. If any residual within-sentence drift ever shows up, the reserved fix is two-pass loudnorm in `lib/dsp.js`.
- `lib/packs.js` (schema-2 sanitizer, dual roots: built-in repo packs + user-data installs) · `lib/packstore.js` (install/export/uninstall/fork, .aegis-meta.json) · `lib/zip.js` (dependency-free, zip-slip/bomb-proof) · `lib/registry.js` (index feeds, update checks, tamper refusal) — pack format is `.dpack`, legacy `.aegispack` accepted.
- **Steam Workshop** (`lib/workshop.js`, PROTOTYPE, shipped 2026-07-15): publish packs to / read subscribed packs from Steam Workshop (WE's channel), via the `steamworks.js` binding (OPTIONAL dependency — native N-API prebuilds win/mac/linux; loaded lazily in MAIN, never renderer). Runs against Valve's public **test AppID 480 (Spacewar)** so the full create/upload/subscribe flow works with NO Steamworks partnership — `steam_appid.txt` (=480, DEV-ONLY, must be deleted for a real Steam build) provides the appid. FAIL-SOFT throughout: no binding or no running Steam client → every path reports Workshop unavailable and the engine is unchanged (verified live: status returned available+real username with Steam running). Publish stages only pack.json+assets (never .aegis-meta or personal data), **renders a real preview image of the dashboard** (off-screen window loading `src/shot.*` with DEMO/empty data so NO personal info is captured — main `renderPackPreview` via `offscreen:true` capturePage, PNG or JPEG if >1 MB; falls back to the pack wallpaper), auto-writes a **rich BBCode description** (persona + component list + author, editable in the dialog), and returns the Workshop URL. IPC `aegis:workshop:status/publish/subscribed/import`; Manager pack-detail has a **Publish to Workshop…** dialog (title/desc/tags/visibility); `importSubscribed` brings a subscribed item into the library via exportPack→installFromBuffer. **The gate is business, not code:** real Workshop needs the app shipped on Steam (Steam Direct $100 + AppID + Valve review). BigInt itemIds cross IPC as strings. Untrusted Workshop packs are safe via the existing sanitizer + Module-SDK sandbox. Consume-side gallery UI (subscribed items as first-class gallery entries) is the main open follow-up; also a real AppID + a Steam build pipeline.
- Voice: `lib/melotts.js` (engine client) / `lib/piper.js` (**re-enabled 2026-08-04 for the shippable MALE voice**) / `dsp.js` / `analyze.js` / `voicebank.js` + `presets/` + `lib/profiles.js`. **Male voices (2026-08-04):** MeloTTS is female-only per language (single speaker; en's 5 "speakers" are just accents), and no commercial male neural TTS covers all 6 langs — so the male option is **Piper's VCTK model** (CC-BY-4.0), speaker **p226 (id 95) = voice `en_male`**, driven via a new `--speaker` flag (`piper.piperArgsForProfile` + `voice.speaker` through `ipc.synthClip`; `voicebank` validates an optional non-negative `speaker`). **Every Latin-script language has a Female↔Male swap** (2026-08-04): en = MeloTTS `en_us_hd` / Piper `en_male` (VCTK p226, CC-BY-4.0); **es = MeloTTS `es_hd` / Piper `es_male`** (davefx, CC0, single-speaker so no `--speaker`); **fr = MeloTTS `fr_hd` / Piper `fr_male`** (UPMC `--speaker 1` = pierre, CC-BY-SA-4.0 — speaker_id_map {jessica:0,pierre:1}, F0-verified male ~126 Hz vs jessica ~230 Hz). **zh/ja/ko stay FEMALE-ONLY** — no commercially-licensable, floor-clearing male neural TTS covers them (Kokoro's Apache-2.0 zh/ja males are Grade C-/D = below floor and it has no Korean; Korean-capable open models are voice-cloning or GPU-3B — see the ⭐ decision note up top). The user swaps male/female by picking that language's preset. Presets: `en-us-male` (clean) + `jarvis` (male + cinematic tuning) + **`es-male` + `fr-male`** (clean); the JARVIS persona auto-selects `en_male` (`PERSONA_VOICE`). Manager voice hint spells out male = en/es/fr, zh/ja/ko female-only. The owner's own standalone JARVIS uses Piper `en_GB-alan` — a great match but PROPRIETARY (Mycroft "All Rights Reserved"), unshippable; `en_GB-vctk` p226 is the licensable stand-in. The voice bank is otherwise **MeloTTS (myshell-ai, MIT) — 6 languages: English, Spanish, French, Chinese, Japanese, Korean** (Piper's 16 voices REMOVED 2026-08-03; MeloTTS is the quality floor the owner accepts — an onnx conversion was below it and was rejected). Each voice = a full MeloTTS (PyTorch) checkpoint + that language's sentence-BERT, downloaded as a per-language directory pack (`en_hd/es_hd/fr_hd/zh_hd/jp_hd/kr_hd`, ~620–880 MB each) rendered by ONE shared frozen engine (`engines.melotts_full`; `engine/melotts/`). The engine is a PyInstaller **`--onedir`** bundle (folder: `melotts-engine.exe` + `_internal/`), **zipped for hosting** (`melotts-engine.zip` ~630 MB, manifest `archive:"zip"`); the app downloads the zip and unpacks it with **bsdtar** (`System32\tar.exe`, ~12 s; Expand-Archive fallback) — `voicebank.downloadEngine`/`extractEngineZip`. onedir (not onefile) means **no ~40 s per-spawn self-extraction** → cold start ~13 s, warm ~1 s. Hosted on **`dashboard-engine/dashboard-engine-hd`** (a neutral HF org — MIGRATED off the personal jh03 repo 2026-08-05; sha256-pinned so the host is interchangeable) (see [[all-melotts-voicebank-plan]]). English HD is meant to be **bundled in the Steam depot** for an instant first-run voice (BUILD.md); the rest download on demand; if nothing is downloaded the OS/system voice covers first-run (fail-soft, `canFallback`). `lib/voicebank.resolveModelDir` prefers a downloaded copy, else a depot-bundled one in `appRoot/voices/<modelDir>`. **Voice tuning is preserved** — a profile's rate/expressiveness/steadiness map to MeloTTS speed/noise_scale/noise_scale_w, then the ffmpeg DSP chain runs (engine-agnostic; `ipc.synthClip` dispatches by `bank.engineOf`). **Hard-won engine facts (do NOT relearn):** the sidecar (`melotts_sidecar.py`) runs ONE language per process and STUBS every non-target `melo.text` frontend + `*_bert` helper — because `get_bert()` imports ALL of them each call and MANY load a tokenizer AT IMPORT, so a frozen/offline engine would crash on a clean machine (it only survived on the dev box because its HF cache held those tokenizers). Per-language keep-lists + bert redirects live in `LANG_CFG`. Two traps: **KR** routes its bert through `japanese_bert` (the generic loader, called with Korean's model_id); **ZH** is a **ZH_MIX_EN** model — MeloTTS remaps language ZH→ZH_MIX_EN (api.py), so it uses the `chinese_mix` frontend (which imports the english + chinese frontends) and **bert-base-multilingual-uncased**, NOT the chinese roberta (the english tokenizer redirects to the SAME multilingual dir so subword counts stay consistent → the pack ships one bert dir). `PYTORCH_JIT=0` required (frozen has no .py for TorchScript). **CWD trap (2026-08-05):** the engine MUST be spawned from a dedicated EMPTY working dir (`lib/melotts.ensureChild` sets `cwd = os.tmpdir()/dashboard-engine-engine-cwd`), NOT the app's launch dir — the frozen Python keeps `''` (CWD) on `sys.path` and nltk's import-security shim aborts startup with "Blocked import of xml.etree from current working directory" when the CWD has shadowing entries; a Steam-launched app has an unpredictable CWD, so an unset cwd made the HD voice fail depending on where it launched (found while verifying the depot bundle). VERIFIED: all 6 synthesize through the FROZEN engine with an EMPTY HF cache (clean-machine sim), and the depot-bundled engine synthesizes from any launch dir. Piper reality is history (it had no ja/ko; the old licence audit stays in `voices.json` `excluded`). The assistant speaks with the profile picked in Manager → Assistant → Voice; localized **persona** presets now match the shipped voices — **es/fr/zh/ja/ko** (de/pt removed 2026-08-04 as they had no HD voice) — so it replies + speaks in-language (`ASSISTANT_PRESETS` in src/manager.js + the `ai-persona-preset` dropdown in manager.html; keep the two in sync). **Persona (reply language) and voice (spoken language) are SEPARATE settings** — a mismatch is the classic bug: a Chinese persona spoken by the English voice comes out as gibberish (English g2p mangling CJK), Spanish reads English-accented. Picking a language persona now AUTO-SELECTS a matching voice (`PERSONA_VOICE` map + `autoSelectVoiceForPersona`). Reply text is stripped of emoji/markdown/`<think>` before TTS (`sanitizeForSpeech`). **Speaker-id trap:** MeloTTS-Chinese's speaker is id 1 (`spk2id={"ZH":1}`), not 0 — the manifest sets `zh_hd sid:1` and the sidecar defensively snaps an out-of-range sid to a real one, else ZH is gibberish (see [[all-melotts-voicebank-plan]]). **Voice presets** (`presets/`, shown in the assistant Voice picker + Voice tuning): 2 per language — a **Broadcast** (formal) and a **Warm** (conversational) profile flavored per language — plus the older character presets re-pointed to `en_us_hd`. Factory presets are selectable directly as the assistant voice (stored `preset:`-prefixed; `ipc.resolveVoiceProfile` loads user profiles OR presets). Picking a voice **pre-warms** its engine in the background (`aegis:voice:prewarm` → `melotts.prewarm`, a tiny throwaway synth) so the first Test/reply isn't a cold-start wait.
- Personal data in `%APPDATA%/dashboard-engine` (auto-migrates from the old `aegis-voice` dir): `settings.json` (active pack), `reminders.json` (`lib/reminders.js` — repeat rules expanded to occurrences in main; `lib/alerts.js` fires desktop notifications for timed events with lead time + 12 h missed-alert catch-up, click opens the planner), `launcher.json` (app pins + engine-tracked recents), `assistant-thread.json` (persisted chat transcript, see the assistant entry), `music.json` (background-music library — see below), installed packs, registries. **Personal data never enters a pack, export, or registry download.**
- **Background music** (shipped 2026-07-26): a GLOBAL setting, NOT a pack component (the owner's explicit call — it was briefly a procedural-ambient component, then a user-files component, then moved to Settings). The user plays THEIR OWN local audio files, configured in **Manager → Settings → Background music** (enable toggle, volume, add-files dialog, removable track list; `lib/music.js` + IPC `aegis:music:list/add/remove/enabled/volume`). Files are REFERENCED, never copied/bundled — like the launcher references apps: main holds the real paths in `music.json` and streams each track to the desktop by OPAQUE id over a gated **`demusic://`** protocol (main.js; `music.pathForId` resolves only ids the user added, path-traversal-proof; the renderer never sees a filesystem path; `media-src demusic:` in the desktop CSP). Playback lives on the desktop surface (`src/dashboard.js`), driven by one predicate — `musicShouldPlay() = enabled && power.active && tracks>0` — reconciled on every Settings change (`aegis:music:changed` broadcast) AND every performance-power signal, so a full-screen app on the PRIMARY monitor pauses it and leaving resumes (secondary-monitor full-screen deliberately doesn't, since the wallpaper is only on the primary). Desktop window gets `autoplayPolicy:'no-user-gesture-required'` so enabling it in Settings starts audio without a desktop gesture. This is media playback of files the user owns — distinct from the voice pipeline's hard "no uploaded audio / no voice cloning" boundary, which is specifically about cloning a voice from a sample.

**Dev surface**
- `npm start` engine · `npm run panel` · `electron . --edit <id>` editor (works against a running engine via single-instance) · `--no-desktop`.
- Tests: `npm run selftest` (boots real app, synthesizes over live IPC) · `smoke` · `packs -- validate/export/install/uninstall` · `voices -- download/verify` · `calibrate` · `audition`.
- Env (legacy `AEGIS_*` still honoured): `DE_PACK`, `DE_VIEW=library|browse|planner`, `DE_SELFTEST`, `DE_PIPER_PATH`, `DE_FFMPEG_PATH`, `DE_SHOT=<dir>` — captures every window via the Electron compositor (occlusion-proof; the ONLY reliable way to screenshot these windows), and `DE_SHOTPREVIEW=<dir>` — renders `DE_PACK`'s Workshop preview image (demo data) to `<dir>/preview.png` and quits.
- `PACKS.md` is the designer-facing doc: authoring, components, styles, `.dpack` export, registry hosting format.

**Hard-won rules (do not relearn these)**
- Every `fs.watch` gets an error handler — deleting a watched dir is an EPERM crash on Windows — and repaints are broadcast directly, never dependent on a watcher surviving. Main has a log-only `uncaughtException` guard (fail-soft).
- Component styling uses cqw container units only (design basis 1920px wide; 1px ≈ 0.0521cqw). A px size inside `.comp` styles breaks editor WYSIWYG and pack portability.
- Two Electron instances must never share a Chromium profile dir (deadlock); tool modes use an isolated one.
- Shells spawned from VS Code inherit `ELECTRON_RUN_AS_NODE=1` — clear it before launching Electron.

**Next milestone candidates (undecided — ask the user before starting)**
- LLM bridge SHIPPED 2026-07-13 (free keyless chat by default via Pollinations + optional OpenAI-compatible endpoint for more models, voice-spoken replies, desktop console). Reply streaming SHIPPED 2026-08-05. Still open: richer curated free-model presets, and the rest of the service widgets — Spotify/YouTube media, audio visualizer, Bluetooth nearby, net-rate telemetry (each needs real infra: OAuth token storage, audio loopback, etc.)
- Official pack registry (format is static-hosting-only; just needs a home + default URL). Steam Workshop is now an ALTERNATIVE/parallel channel (prototype shipped 2026-07-15 against test app 480; see the Workshop entry above) — WE-style, both can coexist. Real Workshop is gated on shipping the app on Steam ($100 Steam Direct + AppID + Valve review), a business decision the owner has NOT yet made — the code path is proven against Spacewar.
- Shell polish: auto-start with Windows, multi-monitor "choose display", editor stage zoom, per-pack user-adjustable properties (WE-style). Performance citizenship (pause on full-screen/battery + fps cap) SHIPPED 2026-07-15 (see above) — first item of the "road to a paid Steam launch" (owner chose flat paid ~$3.99; Steam ownership = the paywall, no gating code). Remaining launch-blockers: multi-monitor, auto-start, per-pack properties, onboarding + a real Settings screen (tray-only for now), DPI/ultrawide hardening, crash reporting.
- Launcher v2: Windows-wide recent apps via UserAssist registry (v1 recents = shell Recent folder + launches through the engine), drag-reorder pins, jump lists. v1 shipped 2026-07-12 — the desktop surface covers icons on 24H2, so the launcher component replaces them (user's explicit call).
- Module SDK (sandboxed HTML packs) SHIPPED 2026-07-15 (see the Module SDK entry above). The hosted marketplace (accounts/payments/moderation) remains out of scope until deliberately chosen. Open follow-ups: a `DE.asset()`-style richer capability surface, a curated module gallery/examples, and possibly per-module user-adjustable properties.

## Stack

- Electron (main + preload + renderer, `contextIsolation: true`, `nodeIntegration: false`)
- Vanilla JS / HTML / CSS in the renderer. No React, no build step, no bundler. Keep it dependency-light.
- Piper (`piper` CLI, ONNX neural TTS) for synthesis — local, offline, CPU
- ffmpeg for the DSP chain
- Node 18+, Windows-first (must not hard-break on macOS/Linux)

## Non-negotiable constraints

- **Voice profiles contain parameters, never audio.** No recordings, no cloned voices, no user-uploaded audio anywhere in the system. A profile is ~1 KB of JSON referencing a licensed base voice plus transformations. This is a legal boundary, not a preference. Do not add an "upload a voice sample" feature under any circumstances.
- **The renderer never touches Node or the OS.** All shell/filesystem/process work happens in the main process behind explicit IPC handlers exposed via `preload.js`. No `nodeIntegration`.
- **Never spawn a shell command built from user text.** Prompts and text go to child processes via stdin, never interpolated into a command line.
- **No browser storage APIs** (`localStorage`, `sessionStorage`) — persist to disk via the main process.
- **Fail soft.** If Piper or ffmpeg is missing, the app must still run and fall back to the OS/system TTS voice with a clear, actionable message. Never crash, never show a raw stack trace to a user.

## Design language

Two registers, never mixed:

**Pack content** — the desktop surface and anything a pack renders. Dark, holographic, technical; this is where the brand look lives (and packs may look like anything at all).
- Palette (reference packs): void `#04080F` · glass `rgba(10,22,35,.55)` · cyan `#3FD8FF` · bright `#7FE9FF` · steel `#5A7E93` · amber `#FFB23E` (warnings) · gold `#E8C56A`
- Type: Rajdhani for display (uppercase, letter-spaced), Share Tech Mono for readouts. These fonts belong to pack content ONLY.
- Glass panels, hairline borders, corner notches, subtle glow, constant-but-subtle motion.

**Engine chrome** — the manager, editor, and voice panel: a quiet, native-feeling dark utility that disappears next to the content it manages. Think well-made desktop tool, not sci-fi console.
- Neutral darks (`#1b1d21` bg · `#242629` panels · `#35383e` borders), text `#e6e8eb` / `#9aa1aa`, one muted accent `#4c8dff`, amber `#e0a446` for warnings only.
- System font stack, sentence case, normal letter-spacing. Monospace (Consolas) only for genuinely numeric/technical values.
- 6px radii, 1px borders, restrained hover states. No glow, no scanlines, no notches, no ambient animation.

Quality floor for both: keyboard focus visible, `prefers-reduced-motion` respected, no layout that breaks under the window minimums.

## Code conventions

- Comment the *why*, not the *what*.
- Small, single-purpose functions. No clever one-liners.
- Config and constants at the top of the file, never buried.
- Every IPC handler validates its input. Assume the renderer is hostile (one day it will be running someone else's pack).
- Prefer clarity over abstraction. We're early; premature layering will hurt.

## Definition of done for any task

- It runs on a clean `npm install && npm start`
- It degrades gracefully with dependencies missing
- No secrets, keys, or absolute personal paths in committed code
- I can hand it to a stranger and they understand it in ten minutes
