{
  "name": "aegis-voice",
  "version": "0.1.0",
  "description": "AEGIS M1 — standalone voice tuning panel. Profiles are parameters, never audio.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "smoke": "node scripts/smoke.js",
    "voices": "node scripts/voices.js",
    "calibrate": "node scripts/calibrate.js",
    "audition": "node scripts/audition.js"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
